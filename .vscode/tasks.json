{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Sync dependencies",
            "type": "shell",
            "command": "pipenv sync --dev",
            "problemMatcher": []
        }
    ]
}
